# This file is used for testing that fields are deserialized to set values
[cluster_config]
configuration_id = 1
nodes = [1, 2, 3]
initial_leader = { n = 1, priority = 2, pid = 1 }

[server_config]
pid = 1
buffer_size = 100000
logger_file_path = "/omnipaxos/logs"
leader_priority = 2
